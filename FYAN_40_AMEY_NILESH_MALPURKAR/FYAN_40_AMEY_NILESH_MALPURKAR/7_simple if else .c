#include <stdio.h>

void main(){
	int a,b;
	printf("enter two numbers a , b :");
	scanf("%d %d",&a,&b);
	
	if(a>b)
	{
		printf ("%d is greater than %d",a);
	}
	else
	{
		printf("%d is not greater",a);
	}
}
